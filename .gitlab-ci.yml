image: microsoft/dotnet:3.0-sdk

stages:
  - build
  
variables:
  project: "zero.sync"

before_script:
  - "dotnet --version"
  - "git submodule init"
  - "dotnet restore"

build:
  stage: build
  variables:
    build_path: "$project"
  script:
    - "cd $build_path"
    - "dotnet build"
